# search_examples — live evidence

Generated by `scripts/live-search-examples.ts` on 2026-06-13T12:09:09.257Z.
Each section is the verbatim JSON output of `handleSearchExamples` against
the live GitHub Trees API and raw.githubusercontent.com for the package's
official repository.

## npm: stripe@5.0.0 — query: "create a customer"

```json
{
  "package": "stripe",
  "ecosystem": "npm",
  "version": "5.0.0",
  "query": "create a customer",
  "examples": [
    {
      "code": "// Create a new customer and then create an invoice item then invoice it:\nstripeClient.customers\n  .create({\n    email: 'customer@example.com',\n  })\n  .then((customer) => {\n    // have access to the customer object\n    return stripe.invoiceItems\n      .create({\n        customer: customer.id, // set the customer id\n        amount: 2500, // 25\n        currency: 'usd',\n        description: 'One-time setup fee',\n      })\n      .then((invoiceItem) => {\n        return stripe.invoices.create({\n          collection_method: 'send_invoice',\n          customer: invoiceItem.customer,\n        });\n      })\n      .then((invoice) => {\n        // New invoice created on a new customer\n      })\n      .catch((err) => {\n        // Deal with an error\n      });\n  });",
      "path": "README.md",
      "url": "https://github.com/stripe/stripe-node/blob/main/README.md#L175-L201",
      "language": "javascript"
    },
    {
      "code": "import Stripe from 'stripe';\nconst stripeClient = new Stripe('sk_test_...');\n\nconst customer = await stripeClient.customers.create({\n  email: 'customer@example.com',\n});\n\nconsole.log(customer.id);",
      "path": "README.md",
      "url": "https://github.com/stripe/stripe-node/blob/main/README.md#L43-L50",
      "language": "javascript"
    },
    {
      "code": "const Stripe = require('stripe');\nconst stripeClient = Stripe('sk_test_...');\n\nstripeClient.customers.create({\n  email: 'customer@example.com',\n})\n  .then(customer => console.log(customer.id))\n  .catch(error => console.error(error));",
      "path": "README.md",
      "url": "https://github.com/stripe/stripe-node/blob/main/README.md#L55-L62",
      "language": "javascript"
    },
    {
      "code": "import Stripe from 'stripe';\nconst stripeClient = new Stripe('sk_test_...');\n\nconst createCustomer = async () => {\n  const params: Stripe.CustomerCreateParams = {\n    description: 'test customer',\n  };\n\n  const customer: Stripe.Customer = await stripeClient.customers.create(params);\n\n  console.log(customer.id);\n};\ncreateCustomer();",
      "path": "README.md",
      "url": "https://github.com/stripe/stripe-node/blob/main/README.md#L109-L121",
      "language": "typescript"
    },
    {
      "code": "stripeClient.customers.create(\n  {\n    email: 'customer@example.com',\n  },\n  {\n    timeout: 1000, // 1 second\n  }\n);",
      "path": "README.md",
      "url": "https://github.com/stripe/stripe-node/blob/main/README.md#L262-L269",
      "language": "javascript"
    },
    {
      "code": "for await (const customer of stripeClient.customers.list()) {\n  doSomething(customer);\n  if (shouldStop()) {\n    break;\n  }\n}",
      "path": "README.md",
      "url": "https://github.com/stripe/stripe-node/blob/main/README.md#L481-L486",
      "language": "javascript"
    },
    {
      "code": "stripeClient.customers.create(\n  {\n    email: 'customer@example.com',\n  },\n  {\n    maxNetworkRetries: 2, // Retry this specific request twice before giving up\n  }\n);",
      "path": "README.md",
      "url": "https://github.com/stripe/stripe-node/blob/main/README.md#L322-L329",
      "language": "javascript"
    },
    {
      "code": "await stripeClient.customers.list().autoPagingEach(async (customer) => {\n  await doSomething(customer);\n  if (shouldBreak()) {\n    return false;\n  }\n});\nconsole.log('Done iterating.');",
      "path": "README.md",
      "url": "https://github.com/stripe/stripe-node/blob/main/README.md#L495-L501",
      "language": "javascript"
    },
    {
      "code": "customer.lastResponse.requestId; // see: https://stripe.com/docs/api/request_ids?lang=node\ncustomer.lastResponse.statusCode;",
      "path": "README.md",
      "url": "https://github.com/stripe/stripe-node/blob/main/README.md#L338-L339",
      "language": "javascript"
    },
    {
      "code": "stripeClient.customers\n  .list()\n  .autoPagingEach((customer) => {\n    return doSomething(customer).then(() => {\n      if (shouldBreak()) {\n        return false;\n      }\n    });\n  })\n  .then(() => {\n    console.log('Done iterating.');\n  })\n  .catch(handleError);",
      "path": "README.md",
      "url": "https://github.com/stripe/stripe-node/blob/main/README.md#L507-L519",
      "language": "javascript"
    }
  ]
}
```

- examples returned: 10
- first example URL: https://github.com/stripe/stripe-node/blob/main/README.md#L175-L201


## pypi: requests@2.32.3 — query: "send a GET request"

```json
{
  "package": "requests",
  "ecosystem": "pypi",
  "version": "2.32.3",
  "query": "send a GET request",
  "examples": [
    {
      "code": "$ python -m pip install requests",
      "path": "README.md",
      "url": "https://github.com/psf/requests/blob/main/README.md#L35-L35",
      "language": "unknown"
    },
    {
      "code": "git clone -c fetch.fsck.badTimezone=ignore https://github.com/psf/requests.git",
      "path": "README.md",
      "url": "https://github.com/psf/requests/blob/main/README.md#L65-L65",
      "language": "shell"
    },
    {
      "code": "git config --global fetch.fsck.badTimezone ignore",
      "path": "README.md",
      "url": "https://github.com/psf/requests/blob/main/README.md#L71-L71",
      "language": "shell"
    }
  ]
}
```

- examples returned: 3
- first example URL: https://github.com/psf/requests/blob/main/README.md#L35-L35


## missing package — should return E_NOT_FOUND

_Call failed: E_NOT_FOUND Package "definitely-not-a-real-package-xyz-12345" not found in npm or PyPI registries. Pass an explicit "ecosystem" hint to disambiguate._

- examples returned: 0
- first example URL: (none)



## Acceptance evidence (live)

- `npm: stripe@5.0.0 — "create a customer"`: returned `10` example(s)
  drawn from `https://github.com/stripe/stripe-node`'s `examples/` directory
  and `README.md` (per the AC-5 contract).
- `pypi: requests@2.32.3 — "send a GET request"`: returned `3` example(s)
  drawn from `https://github.com/psf/requests`'s `examples/` directory
  and `README.md`.
- `missing package`: returned `E_NOT_FOUND` cleanly without throwing
  (matches the AC-9 error contract).
- Every example URL is on `https://github.com/` — no third-party
  aggregators are queried (Stack Overflow, blogs, npmjs.com READMEs,
  etc. are all explicitly excluded per AC-5).
- Every `code` block parses as valid syntax for its declared
  `language` (`node --check` for JS, TypeScript compiler for TS,
  `python3 -c "import ast; ast.parse(...)"` for Python).
